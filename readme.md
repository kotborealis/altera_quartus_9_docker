# Altera Quartus 9 Docker image

Install:
```
npm run-script install
```

Start Quartus 9.1 using `run.sh` script, and specify which directory to mount to `/media` inside container:
```
./run.sh ~
```